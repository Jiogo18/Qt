import QtQuick 2.0
import myLib 1.0

ButtonM {
    id: button
    /*MouseArea {
        anchors.fill: parent;
        onPressed: parent.setMousePressedQML(true, mouseX, mouseY);
        onReleased: parent.setMousePressedQML(false, mouseX, mouseY);
        hoverEnabled: true
        onHoveredChanged: {parent.setHoveredQML(containsMouse, mouseX, mouseY)}
    }*/
}
